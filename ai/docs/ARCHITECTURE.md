# AI-Assisted Automation Platform Architecture

Auto-generated by DocumentationGeneratorAgent v1.0.0
Generated: 2026-06-14T07:55:43.645Z

## Framework Layers

```text
features/ -> step-definitions/ -> pages/ -> BasePage -> Playwright browser
mobile/android/ -> mobile/ios/ -> MobileBasePage -> Appium/WD driver
ai/agents/ -> 42 specialized agents -> ai/output/
```

## Agent Architecture

```text
┌──────────────────────────────────────────────────┐
│                   User / CI/CD                   │
└──────────────────────┬───────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────┐
│            Agent Registry (42 agents)             │
│  ai/agents/index.js                              │
└──────┬──────────┬──────────┬─────────────────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌───▼──────┐
  │Generation│ │Analysis │ │Execution  │
  │ Agents   │ │ Agents  │ │ Agents    │
  └─────────┘ └────────┘ └──────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌───▼──────┐
  │ Mobile  │ │Pipeline│ │Dashboard  │
  │ Agents  │ │ Agents │ │ Agents    │
  └─────────┘ └────────┘ └──────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌───▼──────┐
  │Security │ │Smart   │ │Ecosystem  │
  │ Vault   │ │Selector│ │Readiness  │
  └─────────┘ └────────┘ └──────────┘
```

## Agent Categories

### Generation (7 agents)

- **API Test Generation Agent** (`apiTestGeneration`) v1.0.0 — methods: role, promptFile, outputType, llm, memory
- **FeatureFileGenerationAgent** (`featureFileGeneration`) v1.0.0 — methods: 
- **MobileTestGenerationAgent** (`mobileTestGeneration`) v1.0.0 — methods: run
- **PageObjectGenerationAgent** (`pageObjectGeneration`) v1.0.0 — methods: 
- **StepDefinitionGenerationAgent** (`stepDefinitionGeneration`) v1.0.0 — methods: 
- **TestCaseGenerationAgent** (`testCaseGeneration`) v1.0.0 — methods: 
- **TestDataPipelineAgent** (`testDataPipeline`) v1.0.0 — methods: generateProfile, writeDataset, run, listDatasets, getRunHistory, loadDataset

### Analysis & RCA (5 agents)

- **AnomalyDetectionAgent** (`anomalyDetection`) v1.0.0 — methods: run, getActiveAlerts, getAllAlerts, resolveAlert, getAnalysisHistory
- **Failure Analysis Agent** (`failureAnalysis`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, analyzeWithRag, analyzeWithVectorDb, run
- **PlaywrightCodeReviewAgent** (`playwrightCodeReview`) v1.0.0 — methods: 
- **reportSummarization** (`reportSummarization`) v1.0.0 — methods: run, agent
- **Root Cause Analysis Agent** (`rootCauseAnalysis`) v1.0.0 — methods: role, promptFile, outputType, llm, memory

### Healing & Pipeline (3 agents)

- **Locator Healing Agent** (`locatorHealing`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, suggestWithRag, suggestWithVectorDb, analyzeFailures, applySafeFixes, rollback, run
- **SelfHealingAutomationAgent** (`selfHealingAutomation`) v1.0.0 — methods: 
- **SelfHealingPipelineAgent** (`selfHealingPipeline`) v1.0.0 — methods: _runPlatformTests, runSelfHealingCycle, _executeHealing, runCrossPlatform, run, getRunHistory, getCurrentRun

### Execution & Orchestration (9 agents)

- **Decision Agent** (`decision`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, run
- **execution** (`execution`) v1.0.0 — methods: run, getRunHistory, getCurrentRun
- **Execution Memory Agent** (`executionMemory`) v1.0.0 — methods: historyPath
- **healing** (`healing`) v1.0.0 — methods: run
- **Planner Agent** (`planner`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, run
- **rca** (`rca`) v1.0.0 — methods: run
- **retry** (`retry`) v1.0.0 — methods: run, getRetryHistory
- **TestExecutionAgent** (`testExecution`) v1.0.0 — methods: run
- **UnifiedMCPOrchestratorAgent** (`unifiedOrchestrator`) v1.0.0 — methods: scanAllPhases, indexKnowledgeBase, generateCICDReport, run, getRunHistory, getMCPStatus

### Mobile & Device Farm (3 agents)

- **mobile** (`mobile`) v1.0.0 — methods: run
- **MobileDeviceFarmAgent** (`mobileDeviceFarm`) v1.0.0 — methods: listDevices, checkAppiumHealth, runOnDevice, runParallel, runAndroidEmulator, runIOSSimulator, runAndroidRealDevice, runIOSRealDevice, runOnBrowserStack, runOnSauceLabs, getRunHistory, getActiveRuns, run
- **MobileTestGenerationAgent** (`mobileTestGeneration`) v1.0.0 — methods: run

### Dashboard & Reporting (5 agents)

- **AIDashboardAgent** (`aiDashboard`) v1.0.0 — methods: run, getDashboardPath, getDashboardData
- **pr** (`pr`) v1.0.0 — methods: run
- **prPreparation** (`prPreparation`) v1.0.0 — methods: preparePR
- **report** (`report`) v1.0.0 — methods: run
- **reportSummarization** (`reportSummarization`) v1.0.0 — methods: run, agent

### CI/CD & Infrastructure (4 agents)

- **jenkins** (`jenkins`) v1.0.0 — methods: run
- **JenkinsBuildFailureAnalysisAgent** (`jenkinsBuildFailureAnalysis`) v1.0.0 — methods: 
- **MCP Health Check Agent** (`mcpHealthCheck`) v1.0.0 — methods: configPath
- **monitoring** (`monitoring`) v1.0.0 — methods: run

### Security & Quality (7 agents)

- **api** (`api`) v1.0.0 — methods: run
- **EcosystemReadinessAgent** (`ecosystemReadiness`) v1.0.0 — methods: checkHealth, run, getReadinessHistory, quickStatus
- **impact** (`impact`) v1.0.0 — methods: run
- **release** (`release`) v1.0.0 — methods: run
- **SecretsVaultAgent** (`secretsVault`) v1.0.0 — methods: init, setSecret, getSecret, setProfile, injectToEnv, injectAllToEnv, generateEnvFile, listProfiles, rotateKey, run, getAuditLog, getStatus
- **SmartTestSelectorAgent** (`smartTestSelector`) v1.0.0 — methods: selectTests, run, getSelectionHistory, analyzeChangedFiles, listFeatureRiskProfile, resetHistory
- **visual** (`visual`) v1.0.0 — methods: run
