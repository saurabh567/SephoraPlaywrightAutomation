# AI-Assisted Automation Platform Architecture

Auto-generated by DocumentationGeneratorAgent v1.0.0
Generated: 2026-07-05T09:56:30.834Z

## Framework Layers

```text
features/ -> step-definitions/ -> pages/ -> BasePage -> Playwright browser
mobile/android/ -> mobile/ios/ -> MobileBasePage -> Appium/WD driver
ai/agents/ -> 42 specialized agents -> ai/output/
```

## Agent Architecture

```text
┌──────────────────────────────────────────────────┐
│                   User / CI/CD                   │
└──────────────────────┬───────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────┐
│            Agent Registry (42 agents)             │
│  ai/agents/index.js                              │
└──────┬──────────┬──────────┬─────────────────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌───▼──────┐
  │Generation│ │Analysis │ │Execution  │
  │ Agents   │ │ Agents  │ │ Agents    │
  └─────────┘ └────────┘ └──────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌───▼──────┐
  │ Mobile  │ │Pipeline│ │Dashboard  │
  │ Agents  │ │ Agents │ │ Agents    │
  └─────────┘ └────────┘ └──────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌───▼──────┐
  │Security │ │Smart   │ │Ecosystem  │
  │ Vault   │ │Selector│ │Readiness  │
  └─────────┘ └────────┘ └──────────┘
```

## Agent Categories

### Generation (7 agents)

- **API Test Generation Agent** (`apiTestGeneration`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, metadata
- **FeatureFileGenerationAgent** (`featureFileGeneration`) v1.0.0 — methods: metadata
- **MobileTestGenerationAgent** (`mobileTestGeneration`) v1.0.0 — methods: run, metadata
- **PageObjectGenerationAgent** (`pageObjectGeneration`) v1.0.0 — methods: metadata
- **StepDefinitionGenerationAgent** (`stepDefinitionGeneration`) v1.0.0 — methods: metadata
- **TestCaseGenerationAgent** (`testCaseGeneration`) v1.0.0 — methods: metadata
- **TestDataPipelineAgent** (`testDataPipeline`) v1.0.0 — methods: generateProfile, writeDataset, run, listDatasets, getRunHistory, loadDataset, metadata

### Analysis & RCA (4 agents)

- **AnomalyDetectionAgent** (`anomalyDetection`) v1.0.0 — methods: run, getActiveAlerts, getAllAlerts, resolveAlert, getAnalysisHistory, metadata
- **Failure Analysis Agent** (`failureAnalysis`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, analyzeLocally, analyzeWithRag, analyzeWithVectorDb, run, metadata
- **PlaywrightCodeReviewAgent** (`playwrightCodeReview`) v1.0.0 — methods: metadata
- **reportSummarization** (`reportSummarization`) v1.0.0 — methods: run, agent, metadata

### Healing & Pipeline (3 agents)

- **Locator Healing Agent** (`locatorHealing`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, suggestWithRag, suggestWithVectorDb, analyzeFailures, applySafeFixes, rollback, run, metadata
- **SelfHealingAutomationAgent** (`selfHealingAutomation`) v1.0.0 — methods: metadata
- **SelfHealingPipelineAgent** (`selfHealingPipeline`) v1.0.0 — methods: _runPlatformTests, runSelfHealingCycle, _executeHealing, runCrossPlatform, run, getRunHistory, getCurrentRun, metadata

### Execution & Orchestration (9 agents)

- **Decision Agent** (`decision`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, run, metadata
- **execution** (`execution`) v1.0.0 — methods: run, getRunHistory, getCurrentRun, metadata
- **Execution Memory Agent** (`executionMemory`) v1.0.0 — methods: historyPath, executionStatePath, cucumberReportPath, metadata
- **healing** (`healing`) v1.0.0 — methods: run, metadata
- **Planner Agent** (`planner`) v1.0.0 — methods: role, promptFile, outputType, llm, memory, run, metadata
- **rca** (`rca`) v1.0.0 — methods: run, metadata
- **retry** (`retry`) v1.0.0 — methods: run, getRetryHistory, metadata
- **TestExecutionAgent** (`testExecution`) v1.0.0 — methods: run, metadata
- **UnifiedMCPOrchestratorAgent** (`unifiedOrchestrator`) v1.0.0 — methods: scanAllPhases, indexKnowledgeBase, generateCICDReport, run, getRunHistory, getMCPStatus, metadata

### Mobile & Device Farm (3 agents)

- **mobile** (`mobile`) v1.0.0 — methods: run, metadata
- **MobileDeviceFarmAgent** (`mobileDeviceFarm`) v1.0.0 — methods: listDevices, checkAppiumHealth, runOnDevice, runParallel, runAndroidEmulator, runIOSSimulator, runAndroidRealDevice, runIOSRealDevice, runOnBrowserStack, runOnSauceLabs, getRunHistory, getActiveRuns, run, metadata
- **MobileTestGenerationAgent** (`mobileTestGeneration`) v1.0.0 — methods: run, metadata

### Dashboard & Reporting (5 agents)

- **AIDashboardAgent** (`aiDashboard`) v1.0.0 — methods: run, getDashboardPath, getDashboardData, metadata
- **pr** (`pr`) v1.0.0 — methods: run, metadata
- **prPreparation** (`prPreparation`) v1.0.0 — methods: preparePR, metadata
- **report** (`report`) v1.0.0 — methods: run, metadata
- **reportSummarization** (`reportSummarization`) v1.0.0 — methods: run, agent, metadata

### CI/CD & Infrastructure (4 agents)

- **jenkins** (`jenkins`) v1.0.0 — methods: run, metadata
- **JenkinsBuildFailureAnalysisAgent** (`jenkinsBuildFailureAnalysis`) v1.0.0 — methods: metadata
- **MCP Health Check Agent** (`mcpHealthCheck`) v1.0.0 — methods: configPath, metadata
- **monitoring** (`monitoring`) v1.0.0 — methods: run, metadata

### Security & Quality (7 agents)

- **api** (`api`) v1.0.0 — methods: run, metadata
- **EcosystemReadinessAgent** (`ecosystemReadiness`) v1.0.0 — methods: checkHealth, run, getReadinessHistory, quickStatus, metadata
- **impact** (`impact`) v1.0.0 — methods: run, metadata
- **release** (`release`) v1.0.0 — methods: run, metadata
- **SecretsVaultAgent** (`secretsVault`) v1.0.0 — methods: init, setSecret, getSecret, setProfile, injectToEnv, injectAllToEnv, generateEnvFile, listProfiles, rotateKey, run, getAuditLog, getStatus, metadata
- **SmartTestSelectorAgent** (`smartTestSelector`) v1.0.0 — methods: selectTests, run, getSelectionHistory, analyzeChangedFiles, listFeatureRiskProfile, resetHistory, metadata
- **visual** (`visual`) v1.0.0 — methods: run, metadata
