# AI Workflow Examples & CLI Commands

Auto-generated by DocumentationGeneratorAgent v1.0.0
Generated: 2026-07-04T19:42:20.132Z

## All Agents CLI Commands

Each agent can be invoked via node:

```bash
node ai/agents/<AgentFileName>.js [command]
```

### AIDashboardAgent (`aiDashboard`)

```bash
# Run with default behavior
node ai/agents/AIDashboardAgent.js

# Available CLI commands depend on agent implementation
```

### AnomalyDetectionAgent (`anomalyDetection`)

```bash
# Run with default behavior
node ai/agents/AnomalyDetectionAgent.js

# Available CLI commands depend on agent implementation
```

### api (`api`)

```bash
# Run with default behavior
node ai/agents/APIAgent.js

# Available CLI commands depend on agent implementation
```

### API Test Generation Agent (`apiTestGeneration`)

```bash
# Run with default behavior
node ai/agents/apiTestGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### Decision Agent (`decision`)

```bash
# Run with default behavior
node ai/agents/DecisionAgent.js

# Available CLI commands depend on agent implementation
```

### DocumentationGeneratorAgent (`documentationGenerator`)

```bash
# Run with default behavior
node ai/agents/documentationGeneratorAgent.js

# Available CLI commands depend on agent implementation
```

### EcosystemReadinessAgent (`ecosystemReadiness`)

```bash
# Run with default behavior
node ai/agents/EcosystemReadinessAgent.js

# Available CLI commands depend on agent implementation
```

### execution (`execution`)

```bash
# Run with default behavior
node ai/agents/ExecutionAgent.js

# Available CLI commands depend on agent implementation
```

### Execution Memory Agent (`executionMemory`)

```bash
# Run with default behavior
node ai/agents/executionMemoryAgent.js

# Available CLI commands depend on agent implementation
```

### Failure Analysis Agent (`failureAnalysis`)

```bash
# Run with default behavior
node ai/agents/failureAnalysisAgent.js

# Available CLI commands depend on agent implementation
```

### FeatureFileGenerationAgent (`featureFileGeneration`)

```bash
# Run with default behavior
node ai/agents/featureFileGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### healing (`healing`)

```bash
# Run with default behavior
node ai/agents/HealingAgent.js

# Available CLI commands depend on agent implementation
```

### impact (`impact`)

```bash
# Run with default behavior
node ai/agents/ImpactAgent.js

# Available CLI commands depend on agent implementation
```

### jenkins (`jenkins`)

```bash
# Run with default behavior
node ai/agents/JenkinsAgent.js

# Available CLI commands depend on agent implementation
```

### JenkinsBuildFailureAnalysisAgent (`jenkinsBuildFailureAnalysis`)

```bash
# Run with default behavior
node ai/agents/jenkinsBuildFailureAnalysisAgent.js

# Available CLI commands depend on agent implementation
```

### Locator Healing Agent (`locatorHealing`)

```bash
# Run with default behavior
node ai/agents/locatorHealingAgent.js

# Available CLI commands depend on agent implementation
```

### MCP Health Check Agent (`mcpHealthCheck`)

```bash
# Run with default behavior
node ai/agents/mcpHealthCheckAgent.js

# Available CLI commands depend on agent implementation
```

### mobile (`mobile`)

```bash
# Run with default behavior
node ai/agents/MobileAgent.js

# Available CLI commands depend on agent implementation
```

### MobileDeviceFarmAgent (`mobileDeviceFarm`)

```bash
# Run with default behavior
node ai/agents/MobileDeviceFarmAgent.js

# Available CLI commands depend on agent implementation
```

### MobileTestGenerationAgent (`mobileTestGeneration`)

```bash
# Run with default behavior
node ai/agents/MobileTestGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### monitoring (`monitoring`)

```bash
# Run with default behavior
node ai/agents/MonitoringAgent.js

# Available CLI commands depend on agent implementation
```

### PageObjectGenerationAgent (`pageObjectGeneration`)

```bash
# Run with default behavior
node ai/agents/pageObjectGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### Planner Agent (`planner`)

```bash
# Run with default behavior
node ai/agents/PlannerAgent.js

# Available CLI commands depend on agent implementation
```

### PlaywrightCodeReviewAgent (`playwrightCodeReview`)

```bash
# Run with default behavior
node ai/agents/playwrightCodeReviewAgent.js

# Available CLI commands depend on agent implementation
```

### pr (`pr`)

```bash
# Run with default behavior
node ai/agents/PRAgent.js

# Available CLI commands depend on agent implementation
```

### prPreparation (`prPreparation`)

```bash
# Run with default behavior
node ai/agents/prPreparationAgent.js

# Available CLI commands depend on agent implementation
```

### rca (`rca`)

```bash
# Run with default behavior
node ai/agents/RCAAgent.js

# Available CLI commands depend on agent implementation
```

### release (`release`)

```bash
# Run with default behavior
node ai/agents/ReleaseGateAgent.js

# Available CLI commands depend on agent implementation
```

### report (`report`)

```bash
# Run with default behavior
node ai/agents/ReportAgent.js

# Available CLI commands depend on agent implementation
```

### reportSummarization (`reportSummarization`)

```bash
# Run with default behavior
node ai/agents/reportSummarizationAgent.js

# Available CLI commands depend on agent implementation
```

### retry (`retry`)

```bash
# Run with default behavior
node ai/agents/RetryAgent.js

# Available CLI commands depend on agent implementation
```

### Root Cause Analysis Agent (`rootCauseAnalysis`)

```bash
# Run with default behavior
node ai/agents/rootCauseAnalysisAgent.js

# Available CLI commands depend on agent implementation
```

### SecretsVaultAgent (`secretsVault`)

```bash
# Run with default behavior
node ai/agents/SecretsVaultAgent.js

# Available CLI commands depend on agent implementation
```

### SelfHealingAutomationAgent (`selfHealingAutomation`)

```bash
# Run with default behavior
node ai/agents/selfHealingAutomationAgent.js

# Available CLI commands depend on agent implementation
```

### SelfHealingPipelineAgent (`selfHealingPipeline`)

```bash
# Run with default behavior
node ai/agents/SelfHealingPipelineAgent.js

# Available CLI commands depend on agent implementation
```

### SmartTestSelectorAgent (`smartTestSelector`)

```bash
# Run with default behavior
node ai/agents/SmartTestSelectorAgent.js

# Available CLI commands depend on agent implementation
```

### StepDefinitionGenerationAgent (`stepDefinitionGeneration`)

```bash
# Run with default behavior
node ai/agents/stepDefinitionGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### TestCaseGenerationAgent (`testCaseGeneration`)

```bash
# Run with default behavior
node ai/agents/testCaseGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### Test Data Generation Agent (`testDataGeneration`)

```bash
# Run with default behavior
node ai/agents/testDataGenerationAgent.js

# Available CLI commands depend on agent implementation
```

### TestDataPipelineAgent (`testDataPipeline`)

```bash
# Run with default behavior
node ai/agents/TestDataPipelineAgent.js

# Available CLI commands depend on agent implementation
```

### TestExecutionAgent (`testExecution`)

```bash
# Run with default behavior
node ai/agents/TestExecutionAgent.js

# Available CLI commands depend on agent implementation
```

### UnifiedMCPOrchestratorAgent (`unifiedOrchestrator`)

```bash
# Run with default behavior
node ai/agents/UnifiedMCPOrchestratorAgent.js

# Available CLI commands depend on agent implementation
```

### visual (`visual`)

```bash
# Run with default behavior
node ai/agents/VisualAgent.js

# Available CLI commands depend on agent implementation
```

## Common Workflows

| # | Workflow | Command | Description |
|---|---|---|---|
| 1 | End-to-End Test Generation | `node ai/index.js --all` | Runs all generation agents sequentially (test cases → feature files → step defs → page objects) |
| 2 | Post-Test AI Analysis | `node ai/index.js --post-test` | Analyzes cucumber report, runs failure analysis, and generates execution summary |
| 3 | Unified Orchestration | `node ai/agents/UnifiedMCPOrchestratorAgent.js` | Scans all phases, indexes knowledge base, generates CI/CD integration report |
| 4 | AI Dashboard Generation | `node ai/agents/AIDashboardAgent.js` | Generates unified HTML dashboard with test results, pipeline runs, and AI reports |
| 5 | Self-Healing Pipeline | `node ai/agents/SelfHealingPipelineAgent.js pipeline` | Runs cross-platform self-healing pipeline with automatic retry and locator healing |
| 6 | Anomaly Detection | `node ai/agents/AnomalyDetectionAgent.js` | Scans execution history for flaky tests, performance regressions, and locator decay |
| 7 | Smart Test Selection | `node ai/agents/SmartTestSelectorAgent.js` | Analyzes git changes and selects optimal test suite based on risk profile |
| 8 | Ecosystem Health Check | `node ai/agents/EcosystemReadinessAgent.js` | Validates all 67 health checks across agents, framework, CI/CD, and reporting |
| 9 | Secrets Vault | `node ai/agents/SecretsVaultAgent.js init` | Initializes encrypted credential vault for cloud device farm and API keys |
| 10 | Test Data Generation | `node ai/agents/TestDataPipelineAgent.js` | Generates synthetic test datasets for users, products, addresses, and payments |
| 11 | Device Farm Execution | `node ai/agents/MobileDeviceFarmAgent.js` | Runs tests on built-in emulators, real devices, or BrowserStack/SauceLabs cloud |

## Phase-to-Agent Workflow Map

| Phase | Workflow | Key Command |
|---|---|---|
| Phase 1: Foundation & Base Agents | Core agent architecture, AgentRunner, AgentRegistry, shared memory, and MCP config. | `node ai/agents/PlannerAgent` |
| Phase 2: Test Generation Pipeline | AI-driven generation of test cases, Gherkin features, step definitions, page objects, and API tests. | `node ai/agents/testCaseGeneration` |
| Phase 3: AI Analysis & RCA | Failure analysis, root cause categorization, Playwright code review, and execution report summarization. | `node ai/agents/failureAnalysis` |
| Phase 4: Locator Self-Healing | Automated locator repair using DOM context, failure logs, and screenshot analysis. | `node ai/agents/locatorHealing` |
| Phase 5: Advanced Self-Healing | Advanced locator healing with apply manager and automatic healing application. | `node ai/agents/LocatorApplyManager` |
| Phase 6: Multi-Agent Execution | Coordinated test execution across platforms with retry logic and execution state tracking. | `node ai/agents/execution` |
| Phase 7: Mobile Test Generation | Appium-based mobile test generation for Android/iOS native and hybrid apps. | `node ai/agents/mobileTestGeneration` |
| Phase 8: Device Farm Agent | Cross-platform mobile device farm with emulator, real device, and cloud (BrowserStack/SauceLabs) support. | `node ai/agents/mobileDeviceFarm` |
| Phase 9: Self-Healing Pipeline | AI-powered self-healing test pipeline with automatic retry, healing, and CI/CD integration. | `node ai/agents/selfHealingPipeline` |
| Phase 10: AI Dashboard | Unified HTML dashboard aggregating test results, pipeline runs, device farm data, and AI reports. | `node ai/agents/aiDashboard` |
| Phase 11: Test Data Pipeline | Synthetic test data generation for users, products, addresses, payment methods, and search queries. | `node ai/agents/testDataPipeline` |
| Phase 12: Unified MCP Orchestrator | Consolidated orchestrator managing all 12+ phases, knowledge base indexing, and CI/CD status reporting. | `node ai/agents/unifiedOrchestrator` |
| Phase 13: Anomaly Detection | Real-time anomaly detection for flaky tests, performance regressions, and locator decay patterns. | `node ai/agents/anomalyDetection` |
| Phase 14: Smart Test Selector | Risk-based test selection using code change analysis, feature mapping, and priority-based optimization. | `node ai/agents/smartTestSelector` |
| Phase 15: Ecosystem Readiness | End-to-end ecosystem health validation across 67 checks in 6 categories. | `node ai/agents/ecosystemReadiness` |
| Phase 16: Secrets Vault | AES-256-CBC encrypted credential management with profile system, key rotation, and env injection. | `node ai/agents/secretsVault` |
| Phase 17: Documentation Generator | Auto-generated multi-format documentation covering all agents, phases, CLI commands, and architecture. | `node ai/agents/ai/docs` |