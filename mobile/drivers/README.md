# Mobile Drivers

This folder is reserved for mobile-driver specific adapters.

Current runtime creation is handled by:

```text
framework/mobile/MobileDriverFactory.js
```

Install Appium drivers before real mobile execution:

```bash
npx appium driver install uiautomator2
npx appium driver install xcuitest
```
